{:paths ["src" "test"]
 :tasks {test {:doc "Run the cljc contract + behavioral test under babashaka"
               :extra-paths ["test"]
               :requires ([asme_materials.main-test])
               :task (let [{:keys [fail error]} (clojure.test/run-tests 'asme_materials.main-test)]
                       (System/exit (if (pos? (+ fail error)) 1 0)))}}}
